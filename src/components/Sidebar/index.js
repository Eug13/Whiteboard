import React from "react";
import styles from "./sidebar.css";

const Sidebar = () => (
    <div className="aside" style={ styles }>
        <h2>This is the Sidebar page</h2>
    </div>
);

export default Sidebar;
